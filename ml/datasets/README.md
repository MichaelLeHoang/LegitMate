# datasets

Planned home for labeled URL/domain/page datasets used to train and evaluate a
risk model. ML is explicitly out of scope for v0.1 (which is deterministic and
rule-based) and planned only after the scoring baseline is stable.

No data or code yet.
